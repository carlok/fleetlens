## Summary

## Verification

- [ ] `ruff check .` and `pytest` pass
- [ ] Ansible syntax check and `ansible-lint` pass (if Ansible changed)
- [ ] Tested against a real or throwaway VM (describe)
- [ ] `CHANGELOG.md` updated
- [ ] No host names, IPs, keys, or credentials in the diff
